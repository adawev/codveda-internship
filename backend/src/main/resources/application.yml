spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/codveda_db
    username: postgres
    password: roots

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
